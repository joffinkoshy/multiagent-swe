"""
starter/planner.py

Minimal planner scaffold — we will expand this slowly.
"""

def plan(task: str) -> list:
    """
    Given a task description (string), return a list of steps (strings).
    Currently returns a placeholder step; will be improved iteratively.
    """
    return [f"Placeholder plan for task: {task}"]